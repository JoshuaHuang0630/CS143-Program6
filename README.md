# Josephus Problem
The Josephus Problem is a math problem where people standing in a circle are eliminated according to a given count until there is only one person remaining.

This program will offer a simulation for the progression of the elimination. It will also provide a mathematical proof to verify the result.

The simulation will begin with a circle of people, whose names will be read from a file. User input decides the elimination count.

The simulation also comes with a UI option, which visualizes the progression of the simulation. The UI offers more freedom to customizing the simulation. Unfortunately, the UI experience includes bugs yet to be fixed.

**Note: Code in this solution is AI generated*

## Reflection
The results I got from AI is better than I expected. I started simple, letting it set up the simulation and output to terminal. This file is still available in JosephusSimulation.java. I was surprised that AI added colors to format the terminal output, making it look nice. However, that was never my goal. In class, we have done the Josephus simulation before, so I wanted to add UI elements. I thought about visualizing the circle, and adding more freedom to customize the simulation. I was happy with the visual result, but the program didn't work as expected. The program simulation result wouldn't match the mathematical result.

I would say one benefit to working with AI is that AI has an incredible attention to detail. Whether it is colors, font, position of UI elements, or the appearance of the arrow. Alongside that, AI code had very few bugs in this project.

However, drawbacks to using AI is made obvious with this project. After just one major modification to the program (adding UI), I ran out of free tokens. Aside from that, since generative AI doesn't truly understand code, it completes a solution using excessive amounts of code. Lastly, due to the lack of my involvement in the code, I am unable to make changes to the code in the case there is a bug, which there are a few. The mathematical result of the Josephus problem isn't aligned with the simulation result, and there is a bug with animations when using manual steps in the simulation. Since I didn't write any code, it is extremely difficult for me to fix the bug. With no more free tokens, I could only leave the bug as is.

Although AI can achieve a lot, it feels like it is working out of my control. While I am the one instructing it what to do, I am forced to fully rely on AI to make changes, fix bugs, and add new features. Since AI is still prone to make mistakes, I am lucky to have most things working as intended.